define l = Character(None, image="lucy", kind=bubble)

label kSounds:
    l "Let's practice k sounds."
    call screen cards

label sSounds:
    l "Ready to practice s sounds?"
    call screen cards

label tSounds:
    l "What about t sounds?"
    call screen cards

label nSounds:
    l "Time for N sounds."
    call screen cards